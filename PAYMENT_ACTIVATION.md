# 🚀 מדריך הפעלת תשלומים ופרסומות (Stay Close)

מסמך זה נועד לעזור לך לזכור את הפעולות הנדרשות להפעלת המערכת העסקית של האפליקציה לאחר סיום התהליכים הבירוקרטיים.

---

## 1. הפעלת תרומות ושדרוג (Allpay)

כיום המערכת נמצאת במצב "ממתין". כל הממשק מוסתר או מראה הודעת "בקרוב".

### שלב א': הגדרות שרת (Railway)
לאחר פתיחת החשבון ב-Allpay, עליך להזין ב-Railway את משתני הסביבה הבאים:
* `ALLPAY_API_KEY`: המפתח שקיבלת מהם.
* `ALLPAY_GUID`: המזהה הייחודי של העסק.
* `ALLPAY_URL`: כתובת ה-API של Allpay (לרוב `https://api.allpay.co.il/v1/`).

### שלב ב': הפעלה ללא עדכון אפליקציה
1. הכנס ללוח הניהול של האפליקציה: `/admin`.
2. תחת לשונית **דאשבורד**, גלול למטה ל-"ניהול מערכת וחירום".
3. לחץ על הכפתור הכחול: **"הפעל תרומות (LIVE)"**.

**תוצאה:** באותו רגע, כל המשתמשים (גם אלו שכבר הורידו את האפליקציה) יראו את כפתורי ה-"שדרוג לפרימיום" וה-"תרומה" והם יוכלו לשלם באמת.

---

## 2. הפעלת פרסומות (AdMob)

הקוד כבר מוכן ומכוון ל-Ad Unit האמיתי שלך (`ca-app-pub-1245288761068546/5996451103`).

### שלב א': רישום האפליקציה ב-AdMob
1. ברגע שהאפליקציה באוויר ב-Google Play, הכנס ל-AdMob.
2. תחת "Apps", לחץ על "Add App".
3. בחר "Android" וסמן שהאפליקציה כבר בחנות.
4. חפש את `com.stayclose.app` וחבר אותה.

### שלב ב': אימות והפעלה
1. וודא ב-AdMob שסטטוס ה-App Ads.txt תקין (אם נדרש).
2. בלוח הניהול של האפליקציה (`/admin`), וודא שהכפתור **"הפעל פרסומות"** לחוץ.

---

## 3. איך לבדוק שהכל עובד?

כדי לבחון את הפתרון כרגע (לפני שיש לך Allpay):
1. הכנס ל-`/admin`.
2. לחץ על **"הפעל תרומות (LIVE)"**.
3. עבור לדף ההודעות או לדף הבית - תראה שכפתור הכתר הזהוב הופיע.
4. לחץ עליו - תראה שדף ה-Paywall מציג כעת את חבילות המחיר במקום את הודעת ה-"בקרוב".
5. **חשוב:** לאחר הבדיקה, החזר את המצב ל-**"השבת תרומות"** כדי שמשתמשים לא ינסו לשלם כשזה עוד לא מוכן.

---

## 4. תחזוקה שוטפת
* **מחירים:** ניתן לעדכן את מחירי המנויים (חודשי/שנתי) ישירות מלשונית ה**הגדרות** בלוח הניהול.
* **קופונים:** ניתן ליצור קופונים להארכת תקופת הניסיון (Trial) בלשונית ה**קופונים**.

---

**זכור:** כל שינוי בלוח הניהול משפיע מיידית על כל המכשירים, ללא צורך בהעלאת גרסה חדשה לחנות!

