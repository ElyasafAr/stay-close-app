# רשימת TODO - Stay Close App

## משימות לשיפור האפליקציה

### 1. 🔧 חלון ההגדרות לא משפיע על התכנה
**סטטוס:** ⏳ ממתין  
**תיאור:** חלון ההגדרות לא משפיע על התכנה (שפה, וצבע רקע)  
**פעולות נדרשות:**
- לבדוק את קובץ ההגדרות (`app/settings/page.tsx`)
- לוודא שההגדרות נשמרות ב-`localStorage` או state management
- לוודא שהשינויים מוחלים על כל המסכים (שפה, צבע רקע)
- לבדוק את `state/useSettings.ts` אם קיים

---

### 2. 🎨 ניגוד צבע לא טוב
**סטטוס:** ⏳ ממתין  
**תיאור:** ניגוד צבע לא טוב - צריך לשפר את הנגישות והקריאות של הממשק  
**פעולות נדרשות:**
- לבדוק את קבצי ה-CSS (`app/globals.css`, `*.module.css`)
- לשפר את יחסי הניגודיות (WCAG AA minimum)
- לבדוק את הצבעים על רקעים שונים
- לוודא שהטקסט קריא על כל הרקעים

---

### 3. 📱 במסך של טלפון אין כפתורי ניווט בין מסכים
**סטטוס:** ⏳ ממתין  
**תיאור:** במסך של טלפון אין כפתורי ניווט בין מסכים  
**פעולות נדרשות:**
- להוסיף תפריט ניווט למובייל (hamburger menu או bottom navigation)
- לבדוק אם יש `components/BottomNavigation.tsx` ולשפר אותו
- לוודא שהניווט עובד על מסכים קטנים
- להוסיף responsive design לניווט

---

### 4. 🔔 התראות - תכנון מחדש
**סטטוס:** ⏳ ממתין  
**תיאור:** כל עניין ההתראות צריך לתכנן מחדש - תאריכים, אפשרות לסקג'ולר שבועי ויומי. כרגע ההתראות לא עובדות בכלל!  
**פעולות נדרשות:**
- לבדוק את `components/ReminderModal.tsx` ו-`components/ReminderChecker.tsx`
- להוסיף אפשרות לבחירת תאריך ספציפי
- להוסיף אפשרות לסקג'ולר שבועי (יום בשבוע קבוע)
- להוסיף אפשרות לסקג'ולר יומי (כל יום באותה שעה)
- לבדוק את `backend/main.py` - endpoint של `check_reminders`
- לוודא שההתראות עובדות בפועל (Browser Notifications API)

---

### 5. 🔐 הצפנת מסד הנתונים
**סטטוס:** ⏳ ממתין  
**תיאור:** הצפנת מסד הנתונים לגמרי - מיילים מוצפנים, שמות של אנשי קשר ופרטים מוצפנים וכו'  
**פעולות נדרשות:**
- לבחור שיטת הצפנה (AES-256, Fernet, וכו')
- להצפין שדות רגישים לפני שמירה ב-PostgreSQL:
  - מיילים
  - שמות אנשי קשר
  - פרטים נוספים
- ליצור key management system (שמירת מפתחות הצפנה)
- לעדכן את `backend/models.py` ו-`backend/main.py`
- לוודא שההצפנה/פענוח עובדים נכון

---

### 6. 📲 הפיכה לאפליקציית אנדרואיד
**סטטוס:** ⏳ ממתין  
**תיאור:** הפיכת הכל לאפליקצייה בחנות אנדרואיד - לבדוק האם יש דרך לעשות את זה כ-WEB APP (PWA) או צריך Native  
**פעולות נדרשות:**
- לבדוק אפשרות PWA (Progressive Web App):
  - להוסיף `manifest.json`
  - להוסיף Service Worker
  - לוודא שהאפליקציה עובדת offline
- לבדוק אפשרות Native עם React Native או Capacitor
- לבדוק דרישות Google Play Store
- להחליט על הגישה הטובה ביותר (PWA vs Native)

---

## הערות כלליות

- כל המשימות דורשות בדיקה מעמיקה של הקוד הקיים
- מומלץ לעבוד על משימה אחת בכל פעם
- לבדוק את כל השינויים לפני העלאה ל-Git

---

**עודכן:** $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")

